# Modern MCP Server Requirements (2025)

# MCP SDK - Choose ONE:
# Option 1: FastMCP (Recommended - simpler API)
fastmcp>=2.0.0

# Option 2: Official MCP SDK (More control)
# mcp>=1.0.0

# Core dependencies
pydantic>=2.0.0              # Type validation
httpx>=0.26.0                # Async HTTP client
python-dotenv>=1.0.0         # Environment variables

# PDF Processing
PyMuPDF>=1.23.8              # Fast PDF parsing
# OR pdfplumber>=0.10.0      # Alternative: more accurate tables

# Optional: AI-powered extraction
openai>=1.12.0               # For LLM-based data extraction
anthropic>=0.20.0            # For Claude API

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
black>=23.7.0
ruff>=0.1.0                  # Modern linter (replaces flake8)
mypy>=1.7.0                  # Type checking

# REMOVED (No longer needed):
# - fastapi (replaced by MCP SDK)
# - uvicorn (no HTTP server)
# - slowapi (no rate limiting needed)
# - python-jose (no API keys)
# - passlib (no authentication)
# - crewai (examples moved to separate repo)
# - pyautogen (examples moved to separate repo)
# - langchain (examples moved to separate repo)
# - aiohttp (replaced by httpx)
# - nest-asyncio (not needed)
