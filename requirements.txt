# Modern MCP Server Requirements (2025)
# Updated for Model Context Protocol compliance

# MCP SDK - Core dependency
mcp>=1.0.0

# Core dependencies
pydantic>=2.0.0              # Type validation
httpx>=0.26.0                # Async HTTP client
python-dotenv>=1.0.0         # Environment variables

# PDF Processing
PyMuPDF>=1.23.8              # Fast PDF parsing

# Optional: AI-powered extraction (recommended for production)
openai>=1.12.0               # For GPT-based data extraction
anthropic>=0.20.0            # For Claude-based data extraction

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
black>=23.7.0
ruff>=0.1.0                  # Modern linter (replaces flake8)
mypy>=1.7.0                  # Type checking

# REMOVED (No longer needed for MCP server):
# - fastapi (replaced by MCP SDK)
# - uvicorn (no HTTP server needed)
# - slowapi (no rate limiting needed - MCP handles this)
# - python-jose (no API key auth)
# - passlib (no password hashing)
# - aiohttp (replaced by httpx)
# - crewai (moved to examples repo)
# - pyautogen (moved to examples repo)
# - langchain (moved to examples repo)
# - nest-asyncio (not needed)
# - requests (replaced by httpx)
# - python-multipart (not needed)
